# Опционально: для локальной разработки apps/desktop-host (envDir = корень репо).
# Оверлей в проде использует apps/obs-overlay/.env.production (локальный gateway).

# VITE_BASE_LOGO_URL=http://127.0.0.1:8787
# VITE_API_BASE_URL=
